package com.example.foodpath.repository

import com.example.foodpath.model.Dish

class DishRepository {

    // Sample function to get dishes (from mock data or future API calls)
    fun getDishes(): List<Dish> {
        // For now, return an empty list
        return emptyList()
    }

    // Add more functions for other data operations as needed
}
